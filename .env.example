# Copy to .env for local development. The two databases share nothing, including these.
TARGET_DB_PASSWORD=target_dev_password
AUDIT_DB_PASSWORD=audit_dev_password

TARGET_DATABASE_URL=postgresql://target_admin:target_dev_password@localhost:5433/warden_target
AUDIT_DATABASE_URL=postgresql://audit_admin:audit_dev_password@localhost:5434/warden_audit
